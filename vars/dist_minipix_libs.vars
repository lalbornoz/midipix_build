#
# set -o errexit -o noglob are assumed.
#

pkg_dist_minipix_libs_all() {
	local _libname;
	for _libname in libc libncursesw libpsxscl libu16ports; do
		ex_rtl_fileop cp				\
			"${PREFIX_NATIVE}/lib/${_libname}.so" "${PREFIX_MINIPIX}/lib";
	done;
};

# vim:filetype=sh
